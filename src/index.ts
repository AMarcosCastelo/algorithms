// import { MyLinkedList, NodeItem } from "./linked-list";
// import { verifyIfIsCircle } from "./slow-and-fast-pointer";

// const linkedList = new MyLinkedList();

// Array.from({ length: 10 }).forEach((_, index) => {
//   console.log(index);
//   if (index === 9) {
//     linkedList.insertNodeToTail(linkedList._head);
//     return
//   }

//   linkedList.insertNodeToTail(new NodeItem(index));
// });

// verifyIfIsCircle(linkedList);